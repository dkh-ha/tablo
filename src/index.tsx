import React from 'react';
import ReactDOM from 'react-dom/client';
import OnlineTabloApp from './OnlineTabloApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OnlineTabloApp />
  </React.StrictMode>
);
